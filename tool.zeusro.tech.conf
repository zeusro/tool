# Nginx 配置 - tool.zeusro.tech
# 使用方式: 将 root 改为实际部署路径，或通过 include 引入

server {
    listen 80;
    listen [::]:80;
    server_name tool.zeusro.tech;

    # 静态文件根目录（部署时改为实际路径，如 /var/www/tool）
    root /path/to/tool;
    index index.html;

    # 主页面
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

# HTTPS 配置（使用 Let's Encrypt 等证书时取消注释并配置）
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name tool.zeusro.tech;
#
#     ssl_certificate     /etc/letsencrypt/live/tool.zeusro.tech/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/tool.zeusro.tech/privkey.pem;
#
#     root /path/to/tool;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
#         expires 7d;
#         add_header Cache-Control "public, immutable";
#     }
#
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
# }
#
# HTTP 重定向到 HTTPS（启用 HTTPS 时取消注释）
# server {
#     listen 80;
#     listen [::]:80;
#     server_name tool.zeusro.tech;
#     return 301 https://$server_name$request_uri;
# }
